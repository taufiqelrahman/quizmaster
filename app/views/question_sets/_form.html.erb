<%= form_for(@question_set) do |f| %>
  <% if @question_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_set.errors.count, "error") %> prohibited this question_set from being saved:</h2>

      <ul>
      <% @question_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :question_group_id %><br>
    <% f.text_field :question_group_id %>
    <%= f.select :question_group_id, options_for_select(@question_groups.map{|s|[s.name, s.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
